void printIthNode(Node *head, int i)
{
    int len = 0;
    Node* temp = head;
    while(temp != NULL){
        if(len == i){
            cout << temp -> data << endl;
        }
        temp = temp -> next;
        len++;
        
    }
}
